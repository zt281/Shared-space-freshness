# 期权定价、曲面拟合与参数状态

Type: grilling
Labels: wayfinder:grilling
Status: claimed
Assignee: alan/Codex-option-algorithm
Parent: ../map.md
Blocked by: 04, 05, 06

## Question

已选择逐笔拟合并将其纳入完整报价反应链路后，首版需要哪些期权定价与曲面拟合方法、输入和有效性判断？按品种、期限和策略配置区分哪些参数与模型状态；同一行情触发多个策略时，哪些计算可共享，哪些必须保留独立结果？明确一次行情与其拟合结果、双边报价之间的版本和因果关系，以及拟合失败、行情不完整时交给策略/风控的可观察结果。

以 [运行与性能决议](04-strategy-performance.md) 和已确定的合约语义为约束，至少能为每组 100 个期权的双边报价准备有代表性的性能原型。此问题使用 grill-with-docs、domain-modeling、codebase-design 与 engineer-cpp-option-market-making；需要外部模型事实时另建有界 research 问题。

[工作台决议](05-workbench-workflow.md) 已要求 T 形报价展示并批量编辑逐合约参数。需要明确它与品种/期限参数的继承、覆盖和所有权关系，完整参数版本的原子范围、基于旧版本的提交冲突与生效回报，以及多个策略共享或独立使用参数时的区别。

## Comments

2026-09-14：用户要求继续，由 alan/Codex 认领，使用 wayfinder、grill-with-docs、grilling、domain-modeling 与 codebase-design。已检查当前本地技能目录，未找到 engineer-cpp-option-market-making 或 engineer-hft-cpp-platform；本轮先开展不依赖这两个技能的业务访谈，外部模型事实按需由 research 核查，不将缺失技能视为模型依据。

沿用已确认约束：标的或关联期权每笔行情触发拟合；完整反应链路包含拟合、每组 100 期权双边报价、风控和提交；参数整版校验并在事件边界切换。统一插件/共享空间契约、关键定价结果有效性及订单启动门槛继续有效，不在本项擅自改为定时拟合或仅局部链路计时。

访谈树（尚非决策）：用户已有/指定定价与拟合方法、首版模型输出、单期限与跨期限范围 → 行情/远期/利率等输入与有效性 → 模型参数/校准状态/逐合约覆盖及共享归属 → 版本因果关系、失败恢复及性能原型样本。先明确产品与方法偏好，再对候选作有界第一方核查，不能把所有 CTP 期权默认为同一行权方式或定价公式。

2026-09-14，交接：用户对 Q50–Q52 明确：“我有一套完整的期权做市波动率拟合算法，把这块留给另一个Agent的讨论”。本会话释放认领，由另一个与用户交互的 Agent 继续；本项保持 open，不作已解决或范围排除处理。用户已有算法的内容、代码位置和接口资料尚未提供。

尚未接受的建议：按标的＋到期日独立拟合、相同输入下共享拟合/定价结果、模型或参数不同时独立实例。不得将这些建议当成用户已有算法的实现方式；下一位 Agent 应先了解已有算法的输入、输出、状态与可共享条件，再讨论参数层级、发布版本、失败语义及原型负载。

本项仍阻塞定价性能原型、后端架构和历史数据等相关问题；交接不改变既有逐笔拟合、完整链路测量或统一共享空间要求。本会话后续推进其他可处理问题，不重新认领本项，除非用户要求。

给下一位 Agent 的 [交接文档](../../handover/option-algorithm.md) 已保存到仓库，包含建议技能、背景入口和继续讨论的边界。

2026-09-14，接续交接：用户指定以 `D:\dev\UMM` 源码讨论现有期权算法及插件契约，由 alan/Codex-option-algorithm 认领。使用 wayfinder、grill-with-docs（grilling、domain-modeling）、engineer-cpp-option-market-making 与 codebase-design，交易路径参考 engineer-hft-cpp-platform。当前这些技能可用；此前的技能缺失记录只代表前一会话。

已读取交接、地图及运行、工作台、共享空间、领域和订单执行决议。先只读核查现有算法事实，再讨论迁移范围、算法可替换层级、状态归属与发布契约。UMM 的现有实现不自动成为 Tyche 的已接受设计；此前 Q50–Q52 建议继续待确认。本轮不修改生产代码，不改动其他问题的认领，不提前记录 Answer 或关闭问题。

本轮只读代码事实（路径相对 `D:\dev\UMM\MS_COM\CommAMM5\source`，不是 Tyche 决议）：

- `strategy/BaseStrategy.cpp:2255` 的 `GetQuoteStrategy` 按 `volType` 分派到成员对象，包括 Grid、OH、Curve、Own 等；此处不是独立动态插件注册，实际运行模式尚未确认。
- `strategy/BaseStrategyCommodity.cpp:119` 的期货 tick 分支重置 timer；`:467` 计数到 100 后进入 `CalcVolatilityOnTimer`，`:2229` 调用 `FitWingModel`。现有触发方式与 Tyche 已确认的逐笔拟合有差异，不能把当前实现描述为逐笔拟合，也不能描述为无条件每 100ms 拟合。
- `strategy/QuoteCommon.cpp:1133` 的自动拟合受 `isAutoFit` 控制，`:1235` 的报价风险曲线更新在该条件块之外；拆分拟合时不能遗漏这项状态更新。`common/DataCluster.h:102` 起分别保存基础、对冲、报价及 vol-band 参数。
- `strategy/QuoteAlgoInterface.h:78` 先 Calc 再 StrategyCheck；成交路径另有 StrategyCheck2。Grid 与 OH 的 delta lean 条件不同，不能未经讨论合并为同一种行为。

首轮待回答（建议均未接受）：

- Q53：UMM 中哪些算法行为作为 Tyche 首版保留基线？建议先以 CommAMM5 的 Wing 拟合、定价及用户指定的一种报价模式贯通全链路；其他模式逐项明确必需范围。逐笔拟合沿用现有 Tyche 决议，旧调度逻辑作为差异记录。
- Q54：首版需要独立替换/部署拟合与报价插件，还是先由一个期权策略插件承载内部可替换的拟合、定价和报价模块？建议后者，先避免把 DataCluster 和内部计算调用顺序扩散为平台契约；若存在跨策略共用拟合实例的明确需求，再据此讨论发布与所有权。

Q53–Q54 用户已确认：

- 以 Grid 模式作为主要报价模式。此回答未明确排除其他模式，也未确认旧代码每个细节均须原样保留。
- 多个策略消费同一份拟合结果，各自施加持仓风险偏移（用户原文“风险便宜”，按上下文理解为“风险偏移”）。因此需要明确共享拟合结果的发布与消费契约；此前先由单个策略插件承载整条计算链的建议未被接受。该选择尚未决定进程部署、共享实例划分、风险持仓范围或曲面更新失败策略。

下一层决策：共享拟合实例的范围及配置所有权、策略独立风险偏移的持仓口径、Grid 现有偏移行为的保留范围；其后明确事件/参数/持仓版本关联、失败处理、跨参数集提交及性能原型输入。

补充 Grid 源码事实：`QuoteAlgo_Grid.h:80` 从基础 `curveVol` 设置 `theoVol`，`:88` 加自动 IV 偏移，风险偏移启用时叠加报价曲面与基础曲面的波动率差，并对定价基价施加 delta 调整，最终再加 optionLean；`:121` 独立保存基础曲面定价的 `theo4QC`。`CalcGridVol` 中 `distance * (call.netPosition + put.netPosition) / PTh` 的波动率扣减随后被上述赋值覆盖，但此前的非正值拒绝仍生效。此项是当前静态代码行为，是否保留或调整尚待用户讨论，未作修复。

第二轮待回答（建议均未接受）：

- Q55：共享拟合实例是否默认按标的＋到期日划分，并允许同期限创建使用不同样本/权重/模型配置的多个实例，由策略显式选择？建议如此，以免只因标的期限相同就强制共用不同配置的结果；具体行情与配置身份随后细化。
- Q56：每个策略的持仓风险偏移取本策略归属持仓、策略组汇总持仓还是账户实际持仓？建议默认本策略归属持仓，需要做市与对冲共管风险时显式配置策略组范围；账户实际风险仍用于既定后台风控，不因策略归属分离而漏计。

Q55–Q56 用户已确认：

- 接受默认按标的＋到期日建立拟合实例；同一期限允许不同配置的多个实例，策略显式选择所消费实例。未要求跨期限联合拟合，不把同标的同期限视为足以自动合并实例的条件。
- 持仓风险偏移默认采用本策略归属持仓；需要做市与对冲共同管理风险时显式选择策略组范围，将期货对冲仓位计入净 Delta。账户实际风险继续用于既定后台风控，不因策略采用归属持仓而漏计。
- 该组级净 Delta 是 Tyche 的已接受需求；UMM 当前 `QuoteCommon` 风险聚合只遍历当前 DataCluster 的期权持仓，不应直接当作已经实现此需求。组级各类风险的统一计量、目标值与偏移系数以及版本关联仍待细化。

第三轮待回答（建议均未接受）：

- Q57：拟合样本加入开关、权重和基础模型配置归拟合实例，风险偏移系数、报价 edge 和数量归策略，是否接受此参数所有权划分？建议修改共享拟合配置时展示并明确提交到该拟合实例，影响其全部消费者；仅希望影响某个策略时，创建/选择独立拟合实例。共享参数同样遵循完整版本校验、基础版本冲突检查及事件边界生效。自动拟合输出与人工曲面参数编辑之间的规则继续待定。
- Q58：一次行情触发的报价是否必须消费该次行情对应的拟合结果，而不是任意“最新”结果？建议每个实例按确定顺序处理相关行情，以当时已接收的完整行情视图计算；发布结果关联触发事件、输入视图及拟合配置版本。策略消费该结果，并固定本轮策略参数与所选范围持仓版本，形成可追溯报价；不要求所有策略同时完成。若参数在途切换或成交到达，按既定事件顺序处理并记录所用版本，不混入半版状态。拟合失败及尚未完成时是否允许使用旧曲面的细化留待后续，不将未变化的成功结果视为失败。

Q57–Q58 用户已确认“都按照建议”：

- 样本选择、拟合权重和基础模型配置归拟合实例，修改时明确展示全部受影响消费者；风险偏移系数、报价价差和数量归各策略。仅改变一个策略的拟合方式时，创建或选择独立拟合实例。沿用完整参数校验、基础版本冲突检查及事件边界生效规则。
- 每个拟合实例按确定顺序处理相关行情；结果关联触发事件、当时已接收的行情输入视图及拟合配置版本。策略消费对应结果，固定本轮策略参数和所选范围持仓版本；不任意读取更新事件的结果代替本事件结果，不要求所有策略同时完成。
- 行情输入视图不要求各合约具有相同交易所时间；有效性与新鲜度仍需明确。此决定不承诺多对象同时发布或跨节点全局同时可见。
- 拟合失败/未完成时是否沿用旧曲面、自动拟合与人工编辑的关系、成交触发报价的版本关联仍未决定。

失败与人工参数的源码核查（UMM 当前静态代码，非 Tyche 已接受行为）：

- `QuoteCommon.cpp:1244` 在参数变更、容差内不变或关闭自动拟合而正常完成时均返回 true；`:1192–1199` 的近零行列式分支返回 false，求解后没有显式有限性检查。因此 bool 不能表达事件完成状态、数值变化与全部业务有效性。
- `BaseStrategyCommodity.cpp:2229` 根据 bool 决定是否回写 Wing 参数，但失败不立即从调用者返回；定时器和 tick 的报价有效性路径不同，不能从拟合 false 推导立即停止全部期权报价。
- `BaseStrategyCommodity.cpp:2190` 先读取外部 Wing 参数，`:2205` 接收变更，`:2229` 再拟合，`:2231` 回写。开启自动拟合时，外部提交的 vr/sr/cc/pc 可能在同轮按拟合结果及更新容差再次改变；未见逐参数手工锁定机制。

第四轮待回答（建议均未接受）：

- Q59：本事件拟合失败或未完成时是否允许旧曲面继续报价？建议计算成功但参数未变化仍发布本事件成功结果；未完成时等待对应结果，不沿用旧结果生成本轮新报价。明确拟合失败、必要输入失效或超过明确配置的有效期限时，相关定价依赖判为失效，按既定依赖/共享保护范围暂停新单、尝试撤单；旧结果只作带状态展示。恢复仍遵循既定核对门槛，不覆盖人工/风险停止。具体质量阈值、期限与故障恢复验收值另需明确，不能默认由旧代码 bool 判定。
- Q60：是否保留显式自动/手工曲面模式？建议自动模式逐笔拟合，拟合产出的 vr/sr/cc/pc 不作为可持续覆盖的普通人工参数；修改这些值需显式切换手工模式，手工模式逐笔重算曲线、定价和风险但不自动拟合改参，切回自动时明确恢复自动更新。模式及参数作为拟合实例完整版本生效并影响全部消费者。这是对既有逐笔拟合要求适用范围的待确认补充，未确认前不改变既有要求；部分参数锁定、自动拟合叠加人工基础曲面偏移另待判断是否必需。

Q59–Q60 最新用户输入：

- Q59 原文“明确设计降级或者扯淡模式”。按上下文暂将“扯淡”理解为“撤单”，继续设计可明确选择的降级/撤单处理；用户未接受所有拟合失败一律立即停止的建议。允许降级的故障类别、配置归属、期限与退回撤单的条件均尚未确认，不据此授权无条件使用旧曲面。
- Q60 原文“都保留”：记录保留自动与手工曲面模式。手工模式逐笔重估而不自动改参，自动模式逐笔拟合；完整参数版本及共享消费者影响按前述建议。该回答是否同时要求自动拟合时锁定部分参数尚有歧义，下一轮明确，暂不将部分锁定标为已接受或排除。

第五轮待回答（方案尚未接受）：

- Q61：是否按策略配置“允许有界降级”或“失效即撤单”？建议拟合实例统一发布当前事件的计算状态，由策略选择是否接受降级；只有拟合未成功而当前必要行情、基价、持仓和其他风控依赖仍有效、旧基础曲面未过期且与当前配置兼容时，才可用旧基础曲面参数在当前事件输入下重新估值并施加当前风险偏移。显式记录当前事件、旧曲面来源事件/版本及降级原因；不把它标为当前事件拟合成功。超过最大曲面年龄/连续失败上限、配置不兼容、数值质量检查失败或必要依赖失效时，转入既定停止新单并尝试撤单的路径。降级期间不能用重新估值或新发布版本刷新最后成功拟合时间；恢复正常须新拟合有效并通过既定恢复门槛。同一原生保护范围的既定联动暂停/撤单约束不因某策略允许降级而失效。阈值具体取值留待明确验收。
- Q62：“都保留”是否也包含自动拟合中的部分参数锁定，例如固定 vr、仅拟合 sr/pc/cc？自动和手工两种模式已保留，此问题仅澄清部分锁定需求；若需要，再决定锁定范围、模式版本和求解方式，不能把求解后覆盖参数直接视为等价的受约束拟合。

Q61–Q62 用户确认“都接受”：

- 拟合实例统一发布计算状态，各策略可选择有界降级或失效即撤单。有界降级仅在必要行情、持仓及风控等依赖有效、旧基础曲面未过期且配置兼容时，按当前事件输入重新定价并施加风险偏移；结果明确关联当前事件、旧曲面来源版本及降级原因，不能冒充本事件拟合成功。
- 降级重估不刷新最后成功拟合时间。超过曲面年龄或连续失败上限、必要输入失效时，转入停止相关新单并尝试撤单；既有共享保护范围的联动规则继续有效。具体期限、失败次数、配置兼容判据及数值质量阈值待定。
- 首版保留自动、手工以及自动拟合中的部分参数锁定能力。锁定属于显式拟合配置，在固定参数约束下求解其余参数，不能以完整求解后覆盖锁定值代替。锁定范围和模式切换细节下一轮确定。

锁定及 Grid 偏移的源码依据：`QuoteCommon.cpp:1145–1154` 的拟合列为 `[1, x, I_call*x*x, I_put*x*x]`，`x=log(K/F)`（`:899`），call/put 选择基于 `basePrice<=strike`（`:900`），不直接按 x 正负分列。`:1201–1222` 求解/更新 vr、sr、cc、pc，其余 Wing 系数不是本函数拟合输出；atmF 在外围随基价更新，不能当成纯手工固定参数。`QuoteAlgo_Grid.h:68,79,120` 的 AutoOptionLean 是另外一条生效的持仓价格偏移，独立于曲面 riskLean 开关；其 delta/vega 参数在 `QuoteCommon.cpp:3962,3965,3989,3991` 用作至少为 1 的分母，设为 0 不等于禁用。旧代码可能同时叠加多种持仓响应，恢复 distance/PTh 对报价的作用属于新增行为。

第六轮待回答（建议均未接受）：

- Q63：首版是否允许 vr/sr/cc/pc 四个拟合系数分别锁定任意组合，锁定只约束共享基础曲面而不冻结策略风险偏移？建议如此；自动模式无锁时拟合四项，部分锁时求解其余项，全锁对应手工曲面行为。锁定标记及固定值随拟合实例配置整版生效。Wing 其余参数按各自配置/行情输入职责处理，不扩大成新的拟合维度。
- Q64：Grid 中被覆盖的 distance/PTh 项是否应恢复到最终定价？建议首版以当前实际生效的定价链为基线，保留基础曲面、自动 IV 偏移、曲面风险偏移、Delta 基价调整和价格单位 optionLean；不补入额外 distance/PTh 波动率偏移，并去掉仅由该未用于定价的中间值触发的拒绝，保留实际用于定价输入的有效性校验。若用户需要该额外库存响应，则须作为显式可启用项另定叠加顺序及避免重复施加的规则。此建议改变旧代码前置拒绝行为，必须经用户决定，尚未修改代码。

Q63–Q64 用户最新指示：

- Q63 按建议接受：vr/sr/cc/pc 可分别锁定任意组合；锁定约束共享基础曲面，策略风险偏移仍独立生效；无锁全部拟合、部分锁求解其余、全锁采用手工曲面行为，配置整版生效。
- Q64 保持待决。用户要求“需要在数学上证明旧代码的正确性与否再明确是否拒绝”。先分析实际控制流及公式，证明 distance/PTh 条件与最终定价有效性之间的逻辑关系；明确证明的前提和能否由源码确定业务目标。此前删除残留检查或恢复库存偏移的建议均未接受，不修改 UMM 代码。

Q64 数学核查已完成，决策仍待用户：详见[Grid 库存波动率项与拒绝条件的数学核查](../analysis/grid-volatility-proof.md)，附[可复现数值反例](../analysis/grid-volatility-counterexamples.py)。在当前曲面有效有限等正常域，库存项 h 只进入 Q-h>=1e-6 的前置门槛，实际定价 sigma=C+a+b(Q-C) 不含 h；通过两类反例证明该门槛不是最终波动率为正的必要或充分条件。若它本来是独立库存规则，则在 distance/PTh 为正时等价于一个依赖 Q 的单侧净持仓上限，不能仅由上述证明决定删除。需要确定该项应承担波动率偏移、定价有效性校验还是独立库存限制的业务规范。

验证：独立 Python 计算片段的反例和容差断言全部通过，git diff --check 通过；未编译或执行 UMM、未证明生产状态可达性或最终下单结果、未修改生产代码。异常当前/历史值混用及 NaN/Inf 语义在分析中单列，未用于制造正常域反例。

Q64 业务规范已由用户明确：“改变库存报价”。因此 distance/PTh 项应有实际报价定价作用；将其只作为独立库存上限不满足本次所选目的。此前删除该项及仅保留其他偏移的建议不采用。旧代码在正常域中覆盖该项的行为不符合这一已明确规范；新公式与其他偏移的叠加关系、所用持仓范围及越界处理继续讨论，不能据此宣称旧代码整体策略已被证明错误或直接修改生产代码。

下一轮待回答（建议未接受）：

- Q65：是否采用 sigma=C+a+b(Q-C)-distance*N/PTh，保留其他已生效偏移并只施加一次库存项？建议如此；库存项独立于曲面风险开关 b，distance=0 可禁用，要求有限 distance>=0、PTh>0。N 使用所选风险持仓范围内同执行价 call 与 put 的有符号净手数之和；期货对冲持仓进入净 Delta，不混入这个手数和。Delta 基价调整和价格单位 optionLean 仍按独立路径施加。已在数学核查文档补充该项的局部定价方向性证明，不能用它推断所有偏移叠加后的总方向或最终挂单单调性。
- Q66：库存偏移令最终实际定价波动率非正、非有限或超过明确允许范围时，是否停止相关合约新单并尝试撤单，而不钳位成最小波动率、不静默禁用库存偏移？建议如此；这是当前目标定价失效，不把允许使用旧基础曲面降级解释成可以绕过策略库存项或其他风险依赖。实际影响范围仍遵循既定依赖及共享保护规则，具体有效范围和恢复细节后续明确。

Q65–Q66 用户确认“均按照推荐”：

- 采用最终定价波动率 sigma=C+a+b(Q-C)-distance*N/PTh，其中 C 为共享基础曲面波动率、a 为自动 IV 偏移、Q 为策略报价风险曲面波动率、b 为其风险偏移开关。库存项仅施加一次，独立于 b；distance>=0、PTh>0，并校验有限性，distance=0 可关闭该项。
- N 是已选风险持仓范围内同执行价 call 与 put 的有符号净手数之和，期货对冲持仓进入净 Delta，不进入此期权手数和。保留独立 Delta 基价调整和价格单位 optionLean；最终行为包含多种已明确偏移的叠加，局部单项方向性不等于整体或最终挂单单调性。
- 最终实际使用的波动率非正、非有限或越过允许范围时，停止相关合约新单并尝试撤单，不静默钳位、不自动关闭库存偏移。旧曲面降级不能绕过此检查。依赖和共享保护规则继续约束影响范围；允许范围数值及恢复细节待定。
- 此为 Tyche 的已接受算法契约，UMM 生产源码仍未修改。数学依据见[Grid 数学核查](../analysis/grid-volatility-proof.md)。

共享结果及成交触发的源码核查：

- `QuoteCommon.cpp:593–606` 的基础风险 Greeks 使用 basePrice 与基础 curveVol；Grid `QuoteAlgo_Grid.h:43,82–87,121` 的 theo4QC 使用 baseAll 与基础 curveVol。baseAll 可因 baseTol 不同于 basePrice，不能把二者的理论价不加区分地互换。
- 普通 Grid 成交路径中，`BaseStrategy.cpp:1530` 置 tradePause，`:1538–1547` 刷新策略持仓后进入 OnRtnQty；`:1622` 调 Grid OnTrade，`QuoteStrategy_Grid.h:37` 转 QuoteAfterTrade，`QuoteAlgo_Grid.h:143–145` 返回 false，未回到普通 Calc。其失败分支仅有 OH 等特定处理，不能视作普通 Grid 即时重算或通用即时撤单。
- 后续真正进入常规 Grid 报价时，`:35–39` 清 tradePause 并返回 false，该轮可经 lastCalcReust/QuoteAction 撤单；再后续的正常轮次才重算。此结论限定普通 Grid 连续报价路径，RFQ、NTQ 等另有处理；未回放现场时序。

下一轮待回答（建议未接受）：

- Q67：共享拟合实例是否发布基础曲面参数、逐执行价基础波动率，以及按声明市场输入计算的逐合约市场 IV/基础理论价/Greeks？建议发布这些可共享结果，同时携带对应输入、配置/事件版本、单位和逐项有效性；策略私有地产生风险报价曲面、自动/库存/价格偏移、最终报价。共享理论价或 Greeks 仅在模型、基价、时间利率等输入完全一致时可复用，不能用 basePrice 的理论价替代 baseAll 的 theo4QC；部分市场 IV 失效不自动等同于整个拟合实例失败，按实际依赖判断。
- Q68：成交更新持仓后是否立即触发受影响报价重算而不等待下一笔行情？建议成交先去重并更新归属持仓，使用该事件处理序列中最近可用且配置兼容的曲面与新持仓，重算所有受所选风险范围变化影响的报价；仅内部持仓变化不重复拟合市场曲面。新报价关联成交/持仓事件及所用曲面版本，受影响的旧持仓版本待提交报价作废，已有挂单按既定撤改顺序更新；无有效结果时按已定降级/撤单条件处理，不扩大为可绕过关键依赖检查的许可。订单和成交事件仍逐条处理，不沿用旧 Grid tradePause 的额外跳过一轮行为。

Q67–Q68 用户确认“都接受”：

- 共享拟合实例发布基础曲面参数、逐执行价基础波动率、市场 IV、基础理论价及 Greeks，并明确输入/配置/事件版本、单位和逐项有效性。各策略独立计算报价风险曲面、自动 IV/库存/Delta/价格偏移、最终报价及其依赖版本。
- 共享理论价及 Greeks 只在定价模型、基价、时间利率等输入一致时复用；不把 basePrice 的计算结果直接当作 baseAll 的 theo4QC。
- 成交去重并更新归属持仓后，立即触发所有受所选风险范围变化影响的报价重算，不等下一笔行情。使用事件序列中最近可用、配置兼容的曲面与新持仓；内部持仓变化本身不重复拟合市场曲面。
- 受影响的旧持仓版本待提交报价作废，已有挂单按既定撤改顺序更新；数据失效时执行已定降级/撤单规则。不同事件及拟合/持仓版本仍显式关联，不能因使用最近可用曲面而混入未来事件结果。

共享基础拟合与策略独立报价这一已接受取舍的理由记录为[共享基础拟合结果并独立计算策略报价](../../../docs/adr/0003-shared-fitting-independent-quotes.md)。该 ADR 不将本问题视为已解决，不预选进程部署；具体决策细节仍保存在本问题。

对冲及安全带的源码事实：`BaseStrategy.cpp:617–634` 分别加载 WMP4Hedge/WMPVolBand；基础 FitWingModel 只更新基础四系数及报价风险曲面，未见整体复制给 hedge/band。外围 atmF 可随基价更新，不能把独立参数等同于冻结价格。`QuoteCommon.cpp:177` 使用 hedge 参数和 t 进行 TTM 缩放；`BaseStrategyCommodity.cpp:602–612,637–641` 的 HC_fixed 路径更新归一化状态并按 styId 写回，迁移为共享对象时需消除多写者。`QuoteCommon.cpp:2196` 起 VolBandTrim 独立使用 band 曲面和 baseAll 生成价格范围；算法 Calc 之后才执行 trim，不读报价持仓风险曲面。

下一轮待回答（建议未接受）：

- Q69：对冲曲面与安全带是否保持独立配置及单写者？建议对冲曲面按策略组和品种期限管理，包括其固定 TTM 状态；VolBand 作为按标的/期限/配置区分的独立安全带实例，策略显式引用。两者不随基础自动拟合改写四系数，不被策略库存风险偏移修改；行情基价及到期时间仍依其声明输入更新。共享引用者不能各自回写同一权威状态。安全带价格范围按声明且版本固定的未施加策略风险偏移的定价输入计算，不将风险偏移后的基价/波动率反过来放宽保护。
- Q70：跨对象批量参数提交是否允许部分成功？建议原子单位分别为一个拟合实例、一个策略实例、一个对冲/安全带配置实例；同对象的全局及逐合约参数组成完整版本一起校验生效。跨对象批量提交不承诺全有全无，逐对象报告已生效版本、冲突、失败或结果未明，不以整批成功掩盖部分失败。若用户需要多个对象严格一起切换，则应明确要求并另定协调切换契约，不能默认已有统一共享空间就提供跨对象事务。

Q69–Q70 用户确认“都接受”：

- 对冲曲面按策略组、品种期限管理，统一维护参数及时间归一化状态；VolBand 为策略显式引用的独立安全带实例，同标的期限允许不同配置。二者各有单一写者，基础自动拟合和策略库存偏移不改写其参数，基价和到期时间仍按声明输入更新。安全带不随策略风险偏移一起移动而放宽保护。
- 参数原子单位分别是拟合实例、策略实例、对冲配置和安全带配置；同一对象的全局及逐合约参数作为完整版本一起校验生效。跨对象批量操作允许部分成功，逐项反馈生效版本、冲突或失败，不承诺整批全有全无。

定价输入及参数层级的源码事实：`QuoteCommon.cpp:249–268` 在所选合成价/EMA 无有效值时仅记日志并保留原始基价，配置来源与实际来源可能不同；`:275–283` 的 baseTol 又使 baseAll 可能保留旧值。`StyData.h:221–228` 的 t/r/q 等有成员默认值，其中 r=q=0.015；`BaseStrategy.cpp:934,1862` 根据 GetExpiry、decayType、tOffset 及年化常数更新 t，`StyData.cpp:716–722` 接收 r/q 参数。不能把这些缺省值或回退来源视为 Tyche 无需声明的通用模型事实。现有 dS 和合约 riskLean 开关、optionDeltaLeanCoef 等路径体现不同所有权，未在核查路径中发现通用的“合约未设置则继承同名策略参数”机制。

下一轮待回答（建议未接受）：

- Q71：定价配置是否必须显式声明模型与输入来源？建议每个拟合实例绑定明确的模型及适用合约、基价来源/选择规则、到期时间与计时/年化规则、利率及收益率来源/取值和单位；发布实际使用来源与输入版本，策略派生 baseAll 另行记录且不改写共享输入。默认值只能来自用户明确选定且可见的配置；所选基价失效时不静默改用其他来源，允许回退须预先声明并发布回退状态，受既有有效性/降级契约约束。具体适用产品验证及配置数值另需按输入档案验收。
- Q72：逐合约可覆盖参数是否采用“对象默认值＋显式覆盖”规则？建议每个字段声明所有权及可覆盖层级，仅允许声明可覆盖的字段逐合约设置；未设置才继承，0是显式值，关闭功能用明确开关而非通用数值哨兵。面板同时显示最终值、来源和生效版本，清除覆盖表示恢复继承。拟合样本开关等仍归拟合实例，风险偏移系数/价差/数量等仍归策略；提交按Q70的对象完整版本原子生效，不在对象间形成隐式继承。
