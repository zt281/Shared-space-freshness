# 工作台布局、交互与多窗口容量原型

Type: prototype
Labels: wayfinder:prototype
Status: claimed
Assignee: alan/Codex-workbench
Parent: ../map.md
Blocked by: 05, 11

## Question

通过可操作的粗略原型，与用户共同判断 [工作台决议](05-workbench-workflow.md) 中的面板目录、多屏布局、T 形报价参数编辑、价格梯和长历史图表是否可用，以及在什么显示负载内达到已选刷新与反馈目标。此问题验证界面与容量，不能只以静态截图或库的宣传替代实际操作。

原型使用独立 `prototype/<name>` 分支和目录，按 prototype skill 保留决策资产；采用已选 Electron、TypeScript、Vue 技术方向，但其中的临时实现不直接决定正式模块或组件库。合成/回放数据、模拟后台状态与真实接入的证明范围必须标明，不连接真实交易账户。

用户从分类目录创建多个同类面板，自由停靠、拆成独立窗口并保存工作区；不预设固定 15 或 36 面板的产品布局。以至少两块 3440×1440 屏幕为目标场景，逐级改变窗口与面板数量、同时可见数量、期权链行列数、盘口档位、曲线/点数、输入率和突发负载。单屏或模拟多屏的结果不能冒充真实双屏实测。

检验以下问题，并记录用户对实际原型的选择：

- 紧凑布局下关键数字、账户/策略对象、参数草稿与生效版本是否清楚；同类面板多开与联动是否符合操作习惯。
- 价格梯自动跟随、鼠标进入冻结位置和点击绑定价格是否可靠；切换对象后的重新启用、批量确认及失效状态是否容易辨认。
- 参数编辑与行情刷新并行、版本冲突、历史/实时切换、逐级细化查询是否保持操作连贯；具体列、间距、快捷操作与图表细节通过实际体验收敛。
- 在持续更新、历史查询、新建/关闭/隐藏/重开面板、断连恢复和屏幕变化下，实际刷新率、数据年龄、输入/状态显示延迟、积压及 CPU/GPU/内存如何变化。

输出可复现负载与容量曲线、达到目标的范围和剩余瓶颈，并由用户确认可接受的工作负载与超过容量时的提示/处理方式。需要改变需求时，回指原决议记录修订原因；原型结果不能自动取消后台逐笔计算或完整事件记录要求。

## Comments

用户要求继续，由 alan/Codex-workbench 认领。使用 wayfinder、prototype（UI 分支）及 grill-with-docs 推进。先制作三个可切换的紧凑深色布局，以合成行情和模拟执行状态验证面板创建、对象显示、参数草稿/冲突、价格梯及窗口拆分；随后测量当前设备的可复现显示负载。两块 3440×1440 的真实双屏验收与用户操作结论仍须单独记录，不因原型可运行而关闭问题。

原型代码使用独立 worktree 与 prototype/workbench 分支保留；主工作区仅记录决策和原型入口，不提交本会话此前的规划改动。

### 首轮原型资产与观察

- 原型分支：`prototype/workbench`，提交 `618ac6a58e00806bd5aa428359c9fecbec30d244`；独立目录 `D:/dev/Tyche-workbench-prototype/prototype-workbench`。分支内 `README.md` 给出运行方式，`OBSERVATIONS.md` 与 `evidence/` 保存观察、截图和原始 JSON。
- 入口：在上述目录运行 `npm start`，底栏可选 A 全景网格、B 任务分栏、C 聚焦工作台；支持九类目录、重复创建、标题换位、宽度切换、隐藏、真实 Electron 拆窗/回停靠与工作区保存。三个方案尚未由用户选定。
- 合成数据交互已验证价格梯冻结并捕获价格、断连禁用及恢复后重新启用、参数冲突与重新确认、恢复工作区禁用交易，以及独立窗口拆分。TypeScript/构建通过，最终观察无 renderer pageerror。
- Electron 44.4.1 下载未完成，改用本机缓存的 36.9.5；Vue 3.5.42、Vite 7.3.6，锁文件保留。该选择只用于原型，不是正式产品版本决议。
- 实际检测到单块 3440×1440，100% 缩放；测量窗口视口 1584×961。固定 A 布局、6/12 个挂载面板、32 行/链、600 点/图、200 次合成计算/秒时，约 10 秒样本的接收到双 rAF 代理 p99 为 32.9/33.4 ms。固定 A、24 面板、64 行/链、2000 次合成计算/秒的补测约 201.8 ms，帧间隔 p99 116.7 ms；均仅 4 个面板与当前视口相交，其余仍挂载更新。
- 初轮 24 面板样本期间有一次输入并切换至 B，条件改变，单独保留不作同条件容量比较。输入样本不足，未证明本地反馈 p99 ≤50 ms；双 rAF 不是物理呈现或业务状态显示验收，不能宣称 p99 ≤100 ms 已达标。
- 负载同时改变面板数、行数及合成率，尚未隔离容量阈值。24 面板组合显示明显卡顿；下一步需 profile 全量状态更新、不可见面板及整表渲染，不能因此取消后台逐笔处理。
- 原型限制：参数暂为全局模拟整版编辑，逐合约/多行批改未做；拖动只换位，完整自由分割停靠未做；图表历史是异步合成示意，无真实历史查询。真实双屏、热插拔、多窗口压力、长期资源、通知声音及用户可接受容量仍待验收。

当前状态保持 claimed。待用户实际比较布局并给出选择后，继续补足所选布局的交互和容量实验；上述部分观察不是最终 Answer。

启动修订保存在 `830d58f6d8e2683c86cf5559f647157d7a84a472`：后台启动器不显示控制台，Electron 交互窗口正常显示。浏览器辅助预览地址为 `http://127.0.0.1:5178/?variant=A&prototype=1`，浏览器独立模拟状态不代替桌面多窗口观察。

### 第二轮：按用户反馈改为自由 Dock

用户指出固定布局不符合预期，希望像 rc-dock 演示一样随意组合子窗口。该反馈取代此前待选 A/B/C 的交互方向；本轮保持 Vue，使用 Dockview 8.3.1 制作自由停靠原型，不据此决定生产组件库。

原型提交 `b074c7d4a225020c938451b94d9ff48cac8e446c`（`prototype/workbench`）。运行目录不变，桌面原型已重新启动。现在支持拖标签合并、边缘嵌套分区、分隔线连续缩放、工作台内浮动/回停靠、最大化，以及完整布局自动保存和命名恢复。原生桌面拆窗仍通过按钮返回，不支持跨原生窗口直接拖标签。

`node scripts/observe-dock.mjs` 在独立 Electron 数据目录验证了实际拖拽合并、边缘拆分和缩放；并验证浮动、命名布局分区/浮动几何恢复、回停靠和 renderer 刷新恢复。TypeScript/构建通过，观察无 renderer pageerror。分支内 `evidence/dock/` 保留 JSON 和截图，README 已更新操作方式。

首轮 A/B/C 容量数字仅作历史记录，不能作为 Dock 性能结果。当前保持 claimed，仍待用户实际试用反馈、逐合约/多行编辑与真实双屏容量等后续验证。

第三轮按用户要求加入拖拽时的五方向悬浮停靠导航：居中合并、上下左右分区，方向高亮与落点预览。原型通过公开 dropPositionResolver 自行实现，不引入企业版组件；当前不含全工作台外圈导航。实际鼠标验证五方向分组与几何关系、释放后隐藏，并回归保存恢复等操作，构建通过。证据及重跑脚本在原型的 evidence/compass/ 与 scripts/observe-compass.mjs，仍待用户体验确认。
