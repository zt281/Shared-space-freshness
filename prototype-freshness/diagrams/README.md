# 先看图，再操作

这三张图由 Archify 生成，双击 HTML 文件即可阅读；不用点击状态操作按钮。

1. [整体职责](01-overview.html)：发布者把完整版本放入统一虚拟共享空间，策略读取数据及其有效性。中间方框是逻辑空间，不是一台中央服务器。
2. [数据为什么不会凭空变新](02-freshness.html)：旧数据重收到达和普通心跳不能续期；有真实依据的来源业务核验可以发布新证据。来源核验动作仍是原型候选，真实实现尚待验证。
3. [数据恢复与指令执行](03-order.html)：数据恢复只是第一步，交易核对、用户停止、指令期限、授权代次及风控仍须满足。

依据：原型 `../index.html` 和主工作区的运行诊断、共享数据新鲜度决议。图描述已确认概念及注明的候选模型，不代表已实现的 C++ 部署。图中不展开底层网络细节。

## 交付检查

三张图类型均为 `architecture`，均通过 Archify `deliver`：9/9 showcase 检查，0 错误、0 警告。第三张修正了一处标签位置，其余零修正。具体 specification/artifact SHA-256、字节数与输出路径见 [receipts.json](receipts.json)。

`visual-check` 对三张图均返回 exit 2 / skipped：当前环境没有 Chrome/Chromium，未产生截图。浏览器尺寸、交互及视觉审阅均未完成，不能把静态校验当作视觉验收；对应的 `.visual-check.json` 为工具原始记录。
