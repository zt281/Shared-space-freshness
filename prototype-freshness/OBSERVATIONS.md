# 首轮观察：待用户评审

日期：2026-09-17。范围仅为 JavaScript 纯状态模型，不是共享内存、持久化或性能验收。

使用 Node 26.8.2 解析单文件内两个脚本，并从初始状态依次执行页面内原有的八组引导操作，查看结果。没有添加自动化测试文件或生产代码。

| 场景 | 回放结果 |
| --- | --- |
| 正常与重复提交 | 模拟提交后结果未知；重复执行被阻止；查询后为受理，未被标为成交。 |
| 旧数据重新到达 | 接收时间更新，业务证据年龄仍为 1000ms；心跳不续期，生成指令被拒绝。 |
| 价格不变但来源有效 | 来源业务核验更新证据，价格版本保持；时钟 1100ms 时证据年龄为 400ms，可生成指令。 |
| 半次更新不可见 | 写入草稿时原完整视图继续可见，完整发布后切换版本。 |
| 缺口与主动停止 | 数据与交易核对恢复后，主动停止仍限制新单；用户解除后允许。 |
| 新行情不续期旧指令 | 数据已更新，生成于 0ms、到期于 500ms 的指令在 600ms 被拒绝。 |
| 重启代次隔离 | 新代次快照、交易核对及稳定观察完成后，旧代次指令仍被拒绝。 |
| 时间证据未知 | 未知状态限制新单；快照与时间证据、交易核对和稳定观察完成后允许。 |

浏览器工具返回 `No browser is available`，所以页面布局、点击事件、下载功能及视觉可用性尚未通过真实浏览器检查。脚本可解析和纯模型回放不等于浏览器交互验收。

## 需要用户体验确认

重点体验“价格不变但来源有效”“缺口与主动停止”“新行情不续期旧指令”：业务核验与普通心跳的区别是否清楚，状态变化是否符合预期。界面中的来源业务核验是有可靠证据的模拟动作，真实来源如何产生此证据仍需 C++ 候选实现验证。

下一阶段：根据反馈调整候选状态，再推进真实 C++ 本地双节点及后续目标部署；补充完整成交/对冲账、实际数据时间差、持久化和容量测量。问题单保持 claimed，未写最终 Answer，未选定生产阈值。
