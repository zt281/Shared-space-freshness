# Tyche 交易领域术语

本文记录设计讨论中已经明确的业务术语。接入产品、合约标识和账户间关系随后续讨论补充。

## Language

**共享拟合结果（Shared Fit Result）**：由多个策略共同消费的一份曲面拟合结果，各策略据此独立施加自身的持仓风险偏移。
_Avoid_: 最终策略报价（共享拟合结果不包含各策略独有的报价调整）

**拟合实例（Fitting Instance）**：产生一组曲面拟合结果的独立计算对象，默认面向一个标的和到期日。同一标的和到期日可有不同配置的多个实例，策略显式选择所消费的实例。
_Avoid_: 品种期限（相同品种期限不意味着使用同一组拟合配置和结果）

**拟合配置（Fitting Configuration）**：属于拟合实例的样本选择、拟合权重及基础模型配置；其变更影响该实例的全部消费者。
_Avoid_: 策略报价参数（风险偏移系数、报价价差和数量由各策略独立配置）

**对冲曲面（Hedge Curve）**：按策略组、品种期限管理、用于对冲定价和风险计算的曲面，具有独立参数及时间归一化状态。
_Avoid_: 报价风险曲面（对冲曲面不由策略报价的库存偏移改写）

**报价安全带（Quote Safety Band）**：以独立曲面及其声明输入确定报价约束的规则，由策略显式引用，同标的期限可有不同配置的实例。
_Avoid_: 报价风险偏移（安全带不随策略风险偏移一起移动而放宽保护）

**自动曲面模式（Automatic Curve Mode）**：拟合实例按每笔相关行情执行拟合，由算法更新曲面参数的运行方式。
_Avoid_: 定时拟合（本项目自动模式的触发要求是逐笔相关行情）

**曲面参数锁定（Curve Parameter Lock）**：在自动拟合中显式固定 vr、sr、cc、pc 的任意组合，由算法在这些固定值的约束下求解其余参数。锁定约束共享基础曲面，不冻结各策略独立的风险偏移。
_Avoid_: 拟合后覆盖（事后改回某个参数不能代替在该参数约束下重新拟合）

**手工曲面模式（Manual Curve Mode）**：使用人工设置的曲面参数、随每笔相关行情重新计算曲线和定价而不自动拟合改参的运行方式。
_Avoid_: 冻结报价（固定曲面参数不意味着价格、风险或行情有效性停止更新）

**报价计算轮次（Quote Calculation Cycle）**：策略为一次触发固定所用输入版本并计算报价的一轮处理。正常行情触发时使用该事件对应的拟合结果，降级时另明确关联旧曲面来源；两者均固定本轮策略参数和所选范围持仓版本。
_Avoid_: 最新值拼接（计算中随时读取各对象最新值不能保证其属于本轮选定的输入）

**降级报价（Degraded Quoting）**：当前拟合失效时，经策略显式允许，在必要依赖仍有效且旧基础曲面符合使用条件的限度内，按当前输入重新定价并施加风险偏移的报价方式。结果保留旧曲面来源及降级原因，超过规定限度后停止新单并尝试撤单。
_Avoid_: 沿用旧报价（降级仍需重新估值，不将旧曲面冒充当前事件拟合成功的结果）

**持仓风险偏移（Position Risk Lean）**：策略依据所采用的持仓风险，对报价定价施加的调整；多个策略消费同一份拟合结果时，各自独立施加此调整。
_Avoid_: 共享曲面修改（策略自身的风险调整不等于改写所有消费者使用的拟合结果）

**库存波动率偏移（Inventory Volatility Lean）**：Grid 根据所选持仓范围内同执行价看涨、看跌期权的有符号净手数之和，对最终定价波动率施加的调整；净多仓使该项下调波动率，净空仓使其上调。期货对冲持仓不计入此期权手数和。
_Avoid_: 库存拒绝门槛（本项的目的是真正改变库存报价，而非仅决定能否报价）

**风险偏移持仓范围（Risk Lean Position Scope）**：策略计算持仓风险偏移时采用的归属持仓范围，默认是本策略，可显式选择所属策略组。采用策略组范围时，期货对冲持仓计入该组净 Delta。
_Avoid_: 账户风控范围（报价偏移所选持仓范围不改变账户实际风险检查的范围）

**共享空间（Shared Space）**：Tyche 插件通过统一访问接口交换数据的逻辑空间，跨节点使用同一访问模型，不要求各机器拥有相同的原生指针地址。
_Avoid_: 远程裸指针（不能表达数据可见性与有效性）

**发布版本（Published Version）**：插件完成一份数据或指令后显式发布的完整版本，读者不能观察到该版本的半次更新。单份版本完整不代表多个对象同时更新或所有节点同时可见。
_Avoid_: 任意内存写入（写入尚未发布时不构成对其他读者的完整更新）

**权威状态（Authoritative State）**：由指定模块负责写入和发布、供其他模块据以判断的状态。同一份权威状态只有一个写者，其他模块读取或向其提交变更请求。
_Avoid_: 任意共享副本（副本存在不代表有权修改该状态）

**手工交易（Manual Trading）**：用户逐次发起下单或撤单的交易方式。
_Avoid_: 人工策略（容易与自动策略的人工干预混淆）

**自动策略（Automated Strategy）**：无需用户逐笔确认，即可自主提交和撤销订单的交易规则。
_Avoid_: 自动下单按钮（不能表达持续自主运行）

**多账户（Multiple Trading Accounts）**：同一位使用者管理多个交易账户的使用场景；不代表多位使用者共享系统。
_Avoid_: 多用户（本项目首版为个人使用）

**账户实际持仓（Account Position）**：实际交易账户在通道侧持有的仓位，表达该账户真实的持仓方向和数量。
_Avoid_: 策略持仓（策略内部归属不等于通道认可的独立仓位）

**交易归属账（Trade Attribution Ledger）**：Tyche 按策略或手工交易归属记录的持仓与盈亏，用于区分同一账户内不同交易活动的贡献。
_Avoid_: 独立交易账户（归属划分不会创建独立的通道仓位或资金权限）

**未归属（Unattributed）**：已发现但尚不能确定属于哪个策略或 Tyche 手工交易的持仓或交易，等待明确分配。
_Avoid_: 不计风险（未归属持仓仍属于账户实际持仓）

**原币种账目（Native-currency Accounts）**：按资金或盈亏本身所属币种记录的金额。
_Avoid_: 折算总额（展示币种转换后的数值不能代替原币种余额）

**折算估值（Converted Valuation）**：按注明时间的汇率将原币种金额转换为选定展示币种的估值，用于跨币种展示和汇总。
_Avoid_: 可用保证金（估值汇总不产生跨账户资金或保证金互通）

**策略组（Strategy Group）**：约定可以共同停止的一组自动策略，例如同一标的的做市与对冲策略。单个策略故障的影响范围限定在其所属组内。
_Avoid_: 策略类型（同一类型可以有多个运行实例，类型不决定故障影响范围）

**策略实例（Strategy Instance）**：自动策略的一次具体运行；同一份策略代码可以启动多个实例。容量要求中的策略数量按同时运行的实例数统计。
_Avoid_: 策略种类（不能表达同一策略的多个运行实例）

**主动停止（User Stop）**：用户停止选定策略产生新订单，要求撤销该策略已有挂单并保留持仓；该停止意愿持续到用户再次启动。
_Avoid_: 自动恢复等待（故障核对完成不能撤销用户的主动停止意愿）

**合约发单暂停（Instrument Submission Pause）**：暂停某个策略在某个合约上的自动发单，直到用户显式恢复；暂停本身不代表该范围已有挂单均已撤销。
_Avoid_: 策略全部停止（暂停范围可以只覆盖一个合约）

**依赖急停（Dependency Emergency Stop）**：关键模块失效时，停止所有依赖交易路径产生新订单，并在订单通道可用时尝试撤销相关挂单；保留后台恢复能力，不自动市价平仓。
_Avoid_: 全系统退出（无依赖路径可继续运行，撤单是否完成需核实）

**原生保护范围（Native Protection Scope）**：交易通道实际执行自动撤单等保护动作所覆盖的账户、合约或其他对象集合；同一范围可能包含多个策略及手工挂单。
_Avoid_: 策略归属（内部订单归属不能缩小通道实际保护范围）

**风险停止（Risk Stop）**：累计亏损或回撤越过配置上限后，对相应范围实施并保持的停止状态，需用户明确恢复。
_Avoid_: 单笔拒绝（普通单笔或频率超限可以只拒绝当前请求）

**策略参数版本（Strategy Parameter Version）**：一组完整、经过校验且可追溯的策略参数取值。
_Avoid_: 策略代码版本（代码更新与参数调整是不同操作）

**行情至报单延迟（Tick-to-Order Latency）**：从后端收到行情，到 Tyche 接入模块调用目标下单接口提交请求的经过时间，涵盖完整策略链路。
一次行情引发批量报价时，按本轮最后一次必要报撤单的提交调用点计算完成时间。
_Avoid_: 成交延迟、柜台处理延迟（终点不同）

**双边报价（Two-sided Quote）**：同一合约的买价和卖价。计算 100 个期权的双边报价得到 200 个价格结果，实际报撤单数量另行统计。
_Avoid_: 报单数量（报价结果与提交的订单不是同一个量）

**面板（Panel）**：工作台中承载一类交易功能的可布局单元，例如持仓监控或策略设置；同类功能可以同时打开多个面板。
_Avoid_: 窗口（一个窗口可以容纳多个面板，面板也可以拆成独立窗口）

**工作区（Workspace）**：为一组交易任务保存的面板及其窗口布局，用户可以保存和切换多套工作区。
_Avoid_: 策略组（工作区组织用户看到的界面，不定义策略共同停止的范围）

**T 形报价（T-shaped Option Chain）**：按行权价排列的期权链，行权价居中，左侧为看涨期权，右侧为看跌期权；可配置展示行情、策略、持仓及逐合约参数等列。
_Avoid_: 价格梯（价格梯按单一合约的买卖价格档位组织信息）

**闪电下单（Lightning Order Entry）**：以价格梯为入口的手工交易方式；预设账户与数量后，点击买卖侧直接提交订单，点击挂单发起撤单。
_Avoid_: 自动策略（每次下单或撤单仍由用户操作触发）

**面板联动组（Panel Link Group）**：共享合约或品种期限选择的一组面板；账户和策略操作对象各自固定，不随合约联动切换。
_Avoid_: 策略组（面板联动不定义后台策略生命周期）

**面板目录（Panel Catalog）**：按交易功能分类的面板创建入口；用户从中选择类别，新建该类面板的一个实例。
_Avoid_: 工作区（面板目录用于创建面板，工作区保存已布置的面板与窗口）

**双边做市（Two-sided Market Making）**：以持续提供同一合约买卖两侧订单为目标的自动交易方式；卖方订单所需库存或卖出开仓能力由具体产品与账户规则决定。
_Avoid_: 双边报价计算（计算买卖价格结果不代表已经提交或维持两侧订单）

**卖出开仓（Sell to Open）**：卖出衍生合约并新建或增加空头持仓的交易行为。
_Avoid_: 卖出平仓（卖出已有多头以减少或结束该持仓）

**结果未知（Unknown Outcome）**：请求已尝试提交，但现有证据不足以确定通道是否接受或执行，需继续查询核对的状态；其相关风险占用在消歧前保留。
_Avoid_: 失败（超时或回报缺失不能证明请求未执行）

**建立空头（Establish a Short Position）**：在没有已有多头时，通过借券卖空或衍生品卖出开仓取得空头持仓的业务目标。具体借券和交收义务取决于所选产品。
_Avoid_: 裸空（本项目的该项需求接受自动借券及衍生品，不表示要求无借券安排的证券卖空）

**借券卖空（Borrowed-security Short Sale）**：借入证券后卖出，并承担以后归还所借证券的义务；可交易某证券不代表可以借到该证券。
_Avoid_: 证券出借（持有人向他人提供证券，角色和义务不同）

**合约目录（Instrument Catalog）**：接入市场中可识别的交易合约集合，记录各合约的产品归属和可用状态。
_Avoid_: 活动合约（目录中的合约不一定被当前策略使用）

**合约（Instrument）**：某个环境、交易场所与产品命名空间中可独立识别的交易对象。显示名称相同或关联同一公司、标的资产，不表示是同一合约。
_Avoid_: 行情来源（同一合约可以由不同来源提供行情）

**行情来源（Market Data Source）**：为合约提供行情的来源，包含其真实、模拟或回放性质。
_Avoid_: 合约身份（来源改变不必改变经济合约，环境仍须明确隔离）

**保证金范围（Margin Scope）**：通道实际认可、可共同计算保证金与可用资金的一组资产和持仓的范围。
_Avoid_: 展示分组（按产品或策略分组不会改变通道的保证金规则）

**归属转移（Attribution Transfer）**：显式将逻辑持仓从一个归属转到另一个归属，以确认的参考价和生效时间划分前后贡献并保留历史记录。
_Avoid_: 成交（归属转移不产生真实交易）

**活动合约（Active Instrument Set）**：当前由策略配置选中参与运行的合约集合，受权限、有效状态和已验证容量约束。
_Avoid_: 全市场覆盖（支持发现全部合约不等于同时对全部合约运行策略）

**验证通过记录（Validation Pass Record）**：针对特定策略代码、相关配置和目标通道／产品范围的验证通过证据，是后台允许真实发单所需检查的依据之一，仍须用户显式启用。
_Avoid_: 实盘授权（测试通过本身不代表用户已允许真实发单）
